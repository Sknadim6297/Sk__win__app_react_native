# Leave empty when the site is served from the same Express origin (recommended).
# For `npm run dev` inside /website, Vite proxies /api to the backend.
VITE_API_URL=

# Optional: override the Vite proxy target
# VITE_DEV_API_PROXY=http://127.0.0.1:5000
